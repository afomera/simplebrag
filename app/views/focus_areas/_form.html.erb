<%= form_with(model: focus_area) do |form| %>
  <% if focus_area.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(focus_area.errors.count, "error") %> prohibited this focus_area from being saved:</h2>

      <ul>
        <% focus_area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
